import PulsePlaceholder from "./icons/pulse-placeholder-icon";

const Loading = () => {
  return (
    <div className="h-full flex flex-col items-center justify-center">
      <PulsePlaceholder />
    </div>
  );
};

export default Loading;
